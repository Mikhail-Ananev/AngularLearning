import { IsItemFreshDirective } from './is-item-fresh.directive';

describe('IsItemFreshDirective', () => {
  it('should create an instance', () => {
    const directive = new IsItemFreshDirective();
    expect(directive).toBeTruthy();
  });
});
