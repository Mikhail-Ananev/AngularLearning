export interface CourseInfo {
    id: Number;
    title: String;
    creationDate: Date;
    duration: Number;
    description: String;
}

export interface User {
    id: Number;
    firstName: String;
    lastName: String;
}
